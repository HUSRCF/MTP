name: smoke_text_32
source: inline
max_samples: 32
max_length: 512
texts:
  - "Explain why MoE routing can show locality across adjacent generated tokens."
  - "Write a short Python function that computes recall at k for expert prediction."
  - "Summarize the tradeoffs between cache locality and load balancing in a sparse MoE inference server."
  - "Translate this sentence into French and explain one grammar choice: The model routes each token to a small set of experts."
  - "Give a concise checklist for debugging a GPU kernel performance regression."
  - "Write a short paragraph comparing static scheduling and dynamic scheduling for batched inference."
  - "Explain how quantization can change the bottleneck profile of a neural network serving system."
  - "Create a small table with three reasons why tail latency matters in interactive text generation."
  - "Describe a safe rollback plan for enabling a new inference runtime optimization."
  - "Explain the difference between an attribution diagnostic and a production performance claim."
  - "Write pseudocode for grouping requests by expert id while preserving stable token order."
  - "Give three examples of non-MoE bottlenecks in an autoregressive decoder."
  - "Summarize why per-event Python tracing can distort latency measurements."
  - "Explain how p95 latency can regress even when median latency improves."
  - "Write a compact bug report for a kernel launch configuration that regresses on one GPU but not another."
  - "Compare host-side scheduling overhead and GPU kernel execution time in a serving stack."
  - "Generate a checklist for validating output parity after changing a dispatch order."
  - "Explain why a microbenchmark speedup may fail to improve end-to-end TPOT."
  - "Write a short note on how cache flush settings affect benchmark interpretation."
  - "Describe how to design a fallback gate for an experimental runtime path."
  - "Explain the purpose of a heldout prompt split in performance validation."
  - "Write a small JSON schema for recording per-layer latency summary statistics."
  - "Summarize the difference between materialized reorder and indirect traversal."
  - "List common sources of measurement noise in GPU inference benchmarks."
  - "Explain why engine and sampler residuals should be separated from model-forward timing."
  - "Write a paragraph explaining Amdahl's law for optimizing a decoder stack."
  - "Give an example where optimizing a local kernel hurts overall runtime."
  - "Summarize how shared experts can complicate MoE performance attribution."
  - "Explain the difference between production-like and diagnostic telemetry modes."
  - "Write a concise plan for comparing two runtime variants with repeat-3 evidence."
  - "Describe how to interpret a tail-latency gate failure."
  - "Create a short glossary for TPOT, p95, p99, and generate wall time."
